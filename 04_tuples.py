
"""
una tupla es casi como una lista
solo que esta es inmutable(osea no se pueden cambiar los datos dentro de ello)
"""
### tuple ###
my_other_tuple=tuple()
my_tyuple=("xd","xd","XD","osito","ciego")
### ¿se le pueden ingresar datos? ###
# my_tyuple[2] = "error" este sale error ya que no se le pueden cambiar por un indice las variables
# my_other_tuple.isert si yo hago esto me saldra un error ya que no se pueden isertar valores
### ¿si no se puede insertar que se hace con las tuplas? ###

my_tyuple.index
print(my_tyuple.index("XD"))
print(my_tyuple.count("xd"))

# subtuplas
print()